# frozen_string_literal: true

# Flipflop features for this knapsack, loaded alongside the host app's
# config/features.rb. Declarations are additive: reusing a name the host app
# already defines raises Flipflop::FeatureError.
#
#   feature :my_feature,
#           default: false,
#           description: 'What turning this on does.'
Flipflop.configure do
end
